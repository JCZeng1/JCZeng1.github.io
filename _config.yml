remote_theme: pages-themes/midnight@v0.2.0

plugins:
  - jekyll-remote-theme # This ensures the remote theme plugin is enabled

title: "Your Site Title"
description: "A short description of your site's purpose"

# Optional: You can add other site-specific settings here if needed.
